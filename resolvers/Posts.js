const resolvers = {
    Query: {
        getPosts: async () => {},
        getPostById: async (_, args) => {},
    },
    Mutation: {
        createPost: async () => {},
    },
};

module.exports = resolvers;
